- name: JSONReader
  description: A reader object that returns correctly parsed json from a given file
  sections:
    - type: 10
      title: Method signature
      payload: "export function Reader();"
      css: language-ts
    - type: 0
      title: Functionality
      payload: |-
        The reader will only read the json declaration once. 
        After that it will always return the same instance to save on cpu cycles.
